import { StyleSheet } from "react-native"

export const s = StyleSheet.create({
  container: {
    height: 58,
    maxHeight: 58,
  },
  ingredientsContent: {
    gap: 12,
    paddingHorizontal: 32,
  },
})
